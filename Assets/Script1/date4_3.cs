using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class date4_3 : MonoBehaviour
{/* 
    private void InitImage<T> (T image, T sprite) where T : MonoBehaviour
    {
        if (null == sprite)
        {
            image.gameObject.SetActive(false);
            return;
        }

        image.sprite = sprite;
        image.SetNativeSize();

        RectTransform rtf = image.GetComponent<RectTransform>();
        rtf.anchoredPosition = Vector2.zero;     
    } 

}

public class test
{
    private void InitImage(Image image, Sprite sprite)
{
    if (null == sprite)
    {
        image.gameObject.SetActive(false);
        return;
    }

    image.sprite = sprite;
    image.SetNativeSize();

    RectTransform rtf = image.GetComponent<RectTransform>();
    rtf.anchoredPosition = Vector2.zero;     
}

private void InitImage(RawImage image, Texture texture)
{
    if (null == texture)
    {
        image.gameObject.SetActive(false);
        return;
    }

    image.SetNativeSize();
    image.texture = texture;

    RectTransform rtf = image.GetComponent<RectTransform>();
    rtf.anchoredPosition = Vector2.zero;
} */
}